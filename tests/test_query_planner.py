from src import QueryPlanner


query_planner = QueryPlanner(planning_steps=5)

result = query_planner.run(query="what will be world with AGI coming and doing all the jobs that humans are doing now. how will it impact economy?")